export class Subscription {
    // registryDate: Date;
    // name: string;
    // lastName: string;
    // email: string;
    // level: number;
    // id: number;
    // category: string;
    // title: string;
    // speaker: string;
    // instituition: string;
    // country: string;

    constructor(
        registryDate: Date,
        name: string,
        lastName: string,
        email: string,
        level: number,
        id: number,
        category: string,
        title: string,
        speaker: string,
        instituition: string,
        country: string
    ) { }
}